pub fn add(sum: i32, incoming: i32) -> i32 {
  return sum + incoming;
}

pub fn sub(sum: i32, incoming: i32) -> i32 {
  return sum - incoming;
}

pub fn divide(sum: i32, incoming: i32) -> i32 {
  return sum / incoming;
}

pub fn multiply(sum: i32, incoming: i32) -> i32 {
  return sum * incoming;
}